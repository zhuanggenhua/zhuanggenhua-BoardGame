---
name: mage-wars-ui-design-memory
description: "Mage Wars 用户纠偏索引与 drift-check。用于回看该游戏设计/实现中的已纠正对象、素材职责和送验前自审；不得覆盖项目 UI、响应式、动效、E2E 或看图规范。"
---

# Mage Wars UI Design Memory

这是 BoardGame 项目内、Mage Wars 设计稿与真实 Board/UI 实现专用的用户纠偏索引 / drift-check。它只记录 Mage Wars 的具体对象、素材职责和用户纠正实例；通用 UI、响应式、动效、E2E、看图和审计方法均以 `.spec/knowledge/standards/` 与对应项目 skill 为准，本 skill 不承载这些规范正文。

## 角色与优先级

- 本 skill 的角色是 `drift-check / evidence`，不是 `canonical-source`。它只能提醒 Mage Wars 专项对象是否被漏掉、职责是否混淆、送验前是否踩过用户纠正项。
- 多端布局、PC/移动同构、手机横屏、是否新增移动专用承载，以 `.spec/knowledge/standards/ui-responsive-layout.md` 为唯一规范正文；不得用本 skill 的底部空间、槽位分离或基础布局自审条款覆盖它。
- 可见 UI 改动门禁以 `.spec/knowledge/standards/ui-change-gates.md` 为准；通用 UI/UX 以 `.spec/knowledge/standards/ui-ux.md` 为准；动效和特效以 `.spec/knowledge/standards/animation-effects.md` 为准；E2E 与截图证据以 `.spec/knowledge/standards/e2e-verification.md` 为准。
- 用户开图由项目 `show-image-to-user` skill 决定，正式图面审计由项目 `ui-audit-loop` skill 决定。本 skill 不授权“每轮截图 / 每轮打开图片”，也不把技术截图落盘解释成可送人工验收。
- 若本 skill 的历史措辞能被解读为要求改布局、改移动端界面锚点、新增承载或顺手重构，应先回到上述规范裁决；确认冲突时修正或删除本 skill 的问题措辞，而不是在更下层加反向覆盖。

## 使用顺序

1. 先按 `.spec/knowledge/README.md` 选择本轮真正命中的规范正文；涉及 PC/移动、横屏、响应式或承载变化时，必须先读 `ui-responsive-layout.md`。
2. 当前任务确实涉及 Mage Wars 设计稿、真实 Board/UI、用户标注图或送验自审时，再读本 skill 作为专项 drift-check。
3. 锁定用户当前指出的**具体对象**，不要把一个局部问题扩大成整套 UI 重排，也不要把特效、技能流程或 E2E 修复顺手升级成界面锚点 / 移动布局改版。
4. 改 Open Design artifact 前，写一段“用户原话 -> 本轮动作”的自审；若本轮不是设计稿 / UI 布局任务，只记录风险或后续项，不直接重构布局。
5. 改 Open Design artifact 前，先更新并消费 `docs/games/mage-wars/design/reference/user-correction-traceability-ledger.md`；它是本主题的纠正覆盖账本 / drift-check，不是独立规范来源。
6. 出图后，按当前任务合同、上位规范、“用户原话反思表”和纠正覆盖账本逐条判定 `PASS / REVISE`；有任一条犯错，不得送人工验收。
7. 用户可见交付默认给图：Open Design 截图、草稿图、标注图或真实 UI 截图；是否打开、何时打开、打开哪些图，必须服从项目看图和 UI 审计规范。规则对象覆盖矩阵、纠正账本和自审结论只作内部证据或最终极短回执。

## 规则到 UI 到实现执行顺序

本节只在 Mage Wars 设计稿、真实 `Board.tsx` / UI 实现、实现截图送人工验收这三类任务中作为专项检查表使用。它约束“规则对象是否被正确承载”，不替代通用 UI、响应式、动效、E2E 或看图规范；任何一段都不得只看最后一张图或只看 E2E 结果。

1. **先锁规则对象**
   - 当前轮次重新读取规则真相源、素材 / atlas 合同、区域锚点合同和本 skill。
   - 写出本轮画面里每个规则对象的现实职责：当前主对象、仍可用候选、可浏览归档、隐藏信息或程序化运行态 UI。
   - 规则没有的概念不得进入 UI 文案、class、aria、审计或汇报；用户口语里的“手牌”只在解释时映射为规则术语 `法术书`，不得上屏。

1.5. **建立规则对象覆盖矩阵**
   - 任何 UI 设计、Open Design artifact、真实 `Board.tsx` 实现或实现截图送验前，必须先建立或更新 `规则对象 -> UI 承载` 覆盖矩阵；不能只靠最后一张图或用户纠正账本倒推有没有漏项。
   - 当前项目默认落点是 `docs/games/mage-wars/design/implementable/rule-to-ui-element-list.md`；它负责把规则、TTS / Workshop、atlas 和素材合同提炼为布局、交互和 UI 元素输入。运行时截图覆盖表只能作为 evidence / drift-check，不能替代这份设计前置清单。
   - 每一行至少写清：规则对象、规则真相源、当前阶段职责、默认可见性、实体锚点、玩家可执行动作、素材来源 / `approved-programmatic-runtime-ui` 裁定、截图验收方式。
   - Mage Wars foundation 最低覆盖行包括：2x3 竞技场区域、法师战场实体、玩家 / 法师提示卡、法师牌详情 / 规则回看入口、双方学徒法师生命 24 / 法力 / 聚魔、己方法术书 6 张、己方已计划法术 2 张、对手已计划卡背、公开弃牌堆正面 + 数量 + 展开入口、隐性结界 / 隐藏信息卡背、攻击骰、效果骰、伤害状态 / 伤害数值、燃烧 token、守卫 token、行动 token、来源对象、合法目标高亮、可移动区域高亮、当前阶段 / 当前动作、回合结束入口。
   - 矩阵中的每一行必须有明确结论：`visible`、`collapsed-with-visible-entry`、`hidden-by-rule`、`out-of-scope`、`blocked` 或 `approved-programmatic`。没有结论的对象按遗漏处理，当前稿不得 `AI_PASS`。
   - 用户纠正账本里的每一条纠正必须回指到矩阵中的至少一行；若找不到对应规则对象，先补规则对象裁定，再改设计或实现。

1.6. **法师对象职责与 v80 基线裁定**
   - `法师战场实体 / 法师本体`：代表法师本人在竞技场中的当前位置、来源 / 目标身份、伤害状态、守卫 / 状态 token 宿主。数字 UI 中不得用密密麻麻写满能力文字的法师规则 / 提示卡当场地本体；场地本体必须优先使用人物可识别素材、模型、standee，或由人物素材裁定出的轻量战场实体标记。主锚点是 `mageZoneId` 所属区域；当该区域还有生物 / 魔物时，法师必须进入同一方的场上单位集合、同一尺寸预算和同一密排算法，不得另起法师层或被场上生物 / 魔物挤没。
   - `玩家 / 法师规则提示卡`：承载法师能力、玩家归属和当前操作归属的规则参考；它不是战场实体，不占竞技场区域锚点。当前 v80 基线按用户反复标注裁定为放在玩家 HUD / 玩家面板；未来若重做布局，必须先说明替代承载如何保持“提示 / 参考”和“战场实体”分离，并经用户重新认可。
   - `法师规则 / 提示卡详情`：从规则提示卡或法师本体打开的放大 inspect / 规则回看层；它引用规则提示卡，不再创造第二个可操作法师实体。
   - TTS / atlas 中同一法师若同时存在文字规则卡与人物 / 肖像 frame，必须先查 `CardID`、图面密度和对象职责；禁止凭变量名把 `card` 用到所有位置。当前 foundation 已知基线：邪术师 `2600`、女祭司 `2605`、兽王 `2606`、巫师 `2603` 是密集文字法师规则 / 提示卡，当前 v80 基线放在玩家 HUD / 规则回看；邪术师 `2601`、女祭司 `2604`、兽王 `2607`、巫师 `2602` 是人物 / 肖像 frame，当前 v80 基线放在竞技场作为法师本体素材。
   - 代码 / 测试 / 审计命名必须拆开：战场实体使用 `mage-entity` / `mage-battle-entity`，HUD 规则提示使用 `mage-hud-rule-card` / `player-rule-hint-card`，放大规则详情使用 `mage-rules-card` / `mage-reference-card`；禁止继续使用 `identity-card` 这类无法区分职责的名称作为验收锚点。

2. **再定唯一实体锚点**
   - 每个可见规则对象只能有一个主实体锚点；选中、已计划、来源、目标、摘要、详情、inspect 都只能引用这个实体，不得画成第二个可操作实体。
   - 场上对象先分配 `zoneId` 和锚点，再决定高亮、token、骰子和结算层。
   - 弃牌堆先裁定公开归档 / 当前候选 / 隐藏信息；公开归档默认正面半露 + 数量，不能用卡背伪装成未知信息。

3. **再落交互链**
   - 主交互默认是 `选来源对象 -> 高亮合法目标本体 -> 点击目标本体 -> 结算层锚定来源 / 目标`。
   - 没有规则或系统授权时，不得常驻确认 / 执行 / 取消。
   - 攻击骰、效果骰、伤害状态、燃烧 token、守卫 / 行动 token 是用户点名的规则信息；设计和实现都不能为了画面干净删掉。按 `ui-change-gates.md` 的主源判别：守卫 / 护卫、燃烧、行动准备等是离散规则身份或后续规则会引用的状态，必须用真实 Mage Wars token 图或等价正式状态物件；守卫状态和守卫动作入口都贴宿主中下方，不做右上角按钮堆、护盾 icon 或小卡片壳。墙体法术的交互目标是两个区域之间的边界，边界热区可以承接选择；但结算后的可见墙体对象必须消费 `sourceSpellCardId` 对应的正式墙法术牌面 / 墙牌素材并贴在该边界上，不得只用泛化彩色墙条、纯 CSS 渐变、文字壳或场上生物卡位冒充。其它单位能力入口不是场上规则牌或 token；当前真实 Board 采用类 Summoner Wars 的触发模型：先点来源单位 / 法师本体，来源本体保持选中 / 来源框；顶部横幅 / 阶段条只承载描述和提示，不承载能力按钮；一级能力按钮进入屏幕中下统一动作 dock，点能力后合法目标回到棋盘 / 场上对象本体高亮。能力按钮不得锚到格子角落、守卫 token 轨、状态 token 轨、顶部描述横幅或用牌面缩略图 / 小卡片壳冒充实体。伤害是连续累计数值，当前真实 Board 默认用本体受伤遮罩 + 类 Summoner Wars 的贴宿主剩余 / 总生命读数承载，读数默认 hover / 聚焦显示，眼睛按钮切换全场常显；不强制复刻物理伤害 token 图，也不得用右下角圆形数字球、爱心 / 护盾等通用 icon 冒充状态或伤害。

4. **实现前逐条消费账本**
   - 真实 `Board/UI` 实现前必须读取 `user-correction-traceability-ledger.md`，并把每一条映射到代码或截图里的承载位置。
   - 只照 v75 最终图的大概构图实现，漏掉用户纠正过的对象、规则术语、隐藏信息、分页样式、公开弃牌堆正面或骰子 / token，一律判为实现未完成。

5. **实现截图再验收**
   - E2E、lint、单测只能证明链路可运行，不能证明 UI 通过。
   - 设计稿和真实 `Board/UI` 的验收截图必须是规则对象齐全的饱和交互态；空开局、无已计划法术、无公开弃牌、无骰子 / token、无结算链的截图只能作为技术诊断，不能作为 UI 验收图。
   - 实现截图必须逐项对照：v75 原图、v75 审计、几何证据、区域锚点合同、本 skill 和纠正覆盖账本。
   - 任一用户点名项缺失、弱化、误用规则术语或低质替代，判 `REVISE`，不得打开给用户人工验收。

## 用户原话反思表

每次设计稿送验前，至少逐条回答：

| 用户原话 / 意图 | 送验前必须反思 |
| --- | --- |
| `分页按钮就保持原样啊` | 如果用户只质疑页码 / 占位比例，不得顺手改变分页按钮的样式、方向、位置和视觉语法。需要改页码时，只改页码或占位，不改按钮本体。 |
| `页码占据这么大空间合理吗` | 问题是页码 / 分页读数的视觉占比是否合理，不是“页码没有职责”。分页可以存在，但不能撑出大空槽或成为主对象。 |
| `为什么要放底下` | 除非用户明确要求，分页按钮不得被挪到最底边、右下角或远离法术书卡列的位置；按钮应贴近法术书浏览本体。 |
| `是没地方放页码吗` | 页码是附属读数，可以并入容量读数、贴在书页边角或放在按钮附近；不能因为找页码位置就牺牲按钮样式或卡牌可读性。 |
| `卡牌是没写费用吗非要你来写` | 卡面已有的名称、费用、类型、射程和效果，不要机械复写成外部 chip；如果看不清，先解决卡面尺寸 / 检视层。 |
| `ui和卡牌内容不要重复的规范是不是不够硬` | 这是通用游戏 UI 卡池 / 组牌硬门禁在 Mage Wars 的执行失守：法术卡正面可读时，卡外可见 UI 不得重复法术名称、类型、法力费用、射程、目标、骰数或正文；组书界面只允许补构筑法术点、合法 / 非法原因、选中状态、操作控件和一处唯一数量 owner。送验前必须用文本扫描和图面自审确认没有把可读卡面重写成列表文字。 |
| `搜索框怎么这么大` | 搜索是法术卡池工具，不是主对象；除非当前任务就是检索，搜索框必须让位给卡牌可读面积和筛选按钮，不能用长示例词撑大，不能被筛选行拉伸成高面板，也不能压缩法术卡池。送验前同时量搜索框宽度和高度。 |
| `席位是控制先后手吗` | 不是。Mage Wars 法术书编辑器编辑的是“这本法术书绑定哪名法师”；玩家位置、先后手和对局归属应在对局 setup / 使用摘要处理，不得在组书编辑器左栏常驻 P1 / P2 或“席位”主控。 |
| `排斥成本 / 下一张到上限 / 再加一张 / 排斥示例 / 上限看能力牌，这些你能理解是废话吗` | 能理解。这类词只是规则类别名或实现者注释，不能帮助玩家决定是否加入、移除或修正法术。Mage Wars 组书页所有卡外状态必须落成可行动信息：具体法术点、倍率、数量上限、剩余可加张数、冲突对象和下一步。禁止把“看能力牌”“示例”“可收录”“未受训”“法师限定”作为正常成功状态；若数据缺失，标为数据缺口并阻止送验。 |
| `多级筛选不同系的法术没有区分` | Mage Wars 法术对象不能只按攻击 / 结界 / 生物 / 魔物 / 装备 / 咒语分一层。组书设计稿必须至少同时提供类型、学派 / 元素、等级 / 构筑成本、打出法力费用、合法性 / 冲突、书内 / 未加入等层级；学派 / 元素来自配置中的 `schoolLine`，缺失时只能从 `typeLine` 抽取正式学派 / 元素词，例如自然、火焰、圣光、黑暗、超魔、原力、治疗、闪电。不得把动物、蝙蝠、手套、靴子、传送门、胸甲等子类型混进学派下拉。 |
| `当前法术书可能有几十个卡牌类型，你就打算四个格子来显示吗` | 不能。标准起始书兽王是 50 个条目 / 67 张，右侧法术书清单必须是可滚动或分页的大集合清单：显示总张数、条目数、显示范围、每条的法术点、`当前 / 上限` 和移除 / 调整动作。四张问题样本只能出现在“需处理项”区，不能冒充整本法术书。 |
| `xN 是什么鬼，不是已经有x/y了吗` | 能理解。上一版把卡池卡图和右侧清单同时显示数量，违反唯一数量 owner。Mage Wars 组书页当前采用类似 Hearthstone 的“中间卡牌收藏册 + 右侧 deck list”结构：卡池卡图只显示卡面；添加动作由卡牌本体点击或 hover / focus 动作层承接；不可用原因可以轻量露出。右侧清单用 `当前 / 上限` 显示数量并承担调整动作。只要清单已经显示 `当前 / 上限`，卡图、缩略图和问题队列一律不得再贴 `xN`、数量角标、每卡常驻按钮或第二套数量文案。 |
| `卡牌有什么占用的吧，怎么没看到x/y表示，卡组限制不止是总牌数吧` | 是。Mage Wars 组书合法性核心不是“67 张”本身，而是法师能力牌的法术点上限、受训 / 未受训 / 相斥学派成本、每张数量上限、职业 / 学派限定、史诗和初级例外。组书页顶部必须显示法术点 `当前 / 上限`，右侧每条必须显示数量 `当前 / 上限` 与本条法术点占用；总张数和条目数只能做次级读数。 |
| `切换当前法师的界面怎么没有` | 最新纠偏后裁定为：Mage Wars setup 页主对象是法术书 / loadout，不是法师卡；标准起始书和玩家命名副本都直接展示，点击法术书同时带出它绑定的法师。构筑器只编辑选中法术书、训练规则、法术点和保存库；不得提供第二套常驻法师切换器，也不得把席位、P1 / P2 或先后手带进组书页。这个禁止不等于新建未绑定法术书时可以跳过绑定法师选择。 |
| `当前法师的位置合适吗，怎么感觉有重复了` | 用户感觉是对的。当前法师身份只能由一个主控件承担；在新流程下，这个主控是构筑器顶部左侧的已选法师上下文 / 规则卡入口，只展示已选法师卡图和名称。不得同时出现 active 法师选项列表、`当前法师：兽王` 文案、`兽王标准书` tab 或其它第二身份 owner。 |
| `我要看角色详情是不是都没地方` | 角色详情入口由已选法师主控本体承接：点击顶部左侧已选法师打开临时详情层，展示法师能力牌原图和必要构筑影响，关闭后回到组书页。不得为了补详情再新增同级 `详情` 按钮、新行控件、第二张身份卡或内部法师切换器。 |
| `新的法术书也是在现有预定义的基础上的，可以取自己的名字，而不是再角落偷偷加 UI` | 问题本质是 Mage Wars 法术书库的信息架构，不是保存按钮位置：标准起始书、官方预定义和玩家命名副本必须作为同一法术书库里的同级对象出现。新建法术书不能是角落空态或空白自组；未绑定的新书必须先选择绑定法师，再以该法师标准起始书作为底稿进入可命名草稿。已有命名副本进入构筑器后，可以更新选中副本，也可以另存成新书。 |
| `应该有个加号来添加新法术书，选中已经存在的也可以修改或者修改为新的，上限10本，具体的可以参考召唤师战争` | 能理解。这不是新增一个孤立按钮，而是 Mage Wars 法术书库保存模型必须完整：选书页法术书库和组书页库入口都要有 `+` 新建入口；`+` 表示新建未绑定法术书，必须先打开一次性绑定法师选择层，选定后用该法师标准起始书进入新草稿；已保存命名副本进入构筑器后既能 `更新选中副本`，也能 `另存新书` 产生新 id；玩家命名副本最多 10 本，数据层必须拒绝第 11 本，UI 和扫描必须暴露上限属性；不得回归 `暂无命名副本` 空态补丁、角落 DIY 管理区、空白自组主路径、隐式沿用当前法师或 `当前法术书` 复写。 |
| `我只有让你改过属性ui的位置吧，现在怎么变这么小了` | 这是授权范围和可读性验收失守：Mage Wars 桌面真实 Board 的玩家 HUD / 属性 UI 只允许按本轮目标移动锚点，不允许因为选中法术、选中单位、目标选择、确认计划或教程步骤自动 compact。PC 基线里 HUD 必须保持完整规则提示卡、法师名、生命 / 法力 / 聚魔读数和可点击放大入口；只有移动 / 极窄视口适配可单独裁定 compact，并要回到通用响应式规范。 |
| `对手提示卡说错了，放右上角` / `玩家生命、提示卡可以上移不占手牌位置吧` / `我自己的血量、提示卡放左下只是不和手牌在一排而已` / `对方计划放右上角吧` | Mage Wars 桌面真实 Board 当前基线改为：己方法师 HUD / 生命 / 法力 / 聚魔 / 规则提示卡锚在左下顶层 HUD，贴近但不进入底部法术书牌列；对手 HUD / 规则提示卡锚在右上；对手已计划卡背也在右上并贴对手 HUD 左侧相邻，但不能进入或遮挡对手 HUD。这里的左下是界面锚点层，不是棋盘实体避让算法；己方 HUD 不得因为 A2 / 首列是否存在场上法师或单位而横向漂移。若 HUD 与当前玩家任务发生冲突，按 `ui-ux.md` / `ui-change-gates.md` 的顶层 HUD 分层处理：外壳和提示卡本体不吃输入、只让放大镜 / 眼睛 / 属性 tooltip 等真实控件命中、同层 HUD 重排、临时折叠 / 透明或当前目标提权。底部主牌区不再给己方 HUD 预留固定列，释放给法术书牌列、分页 rail、已计划法术和确认入口。E2E 必须证明己方 HUD 不与法术书牌列 / 计划槽位同排或相交，底部牌区真实获得宽度 / 卡面尺寸收益，双方 HUD 仍保持 full 而不是因布局挤压进入 compact，且被 HUD 视觉覆盖的 A2 当前可操作实体仍能通过主体点击点命中场上对象，不被提示卡 / 属性底纹吞掉；对手已计划只显示卡背 / 数量、不公开正面或卡名。 |
| `确认计划不要用一个小标签表达已选，而是要直接放计划槽位` | Mage Wars 计划阶段的草稿结果必须进入己方两个计划槽位，槽位里显示真实法术牌本体；法术书卡牌只保留可读卡面、copy 数量和选中描边，不能用 `已选`、`选 N`、角标、按钮进度或日志替代槽位承载。教程和 E2E 必须在确认前截图证明 `1/2` 与 `2/2` 的草稿牌已经在计划槽位。 |
| `02=>04你不觉得奇怪吗，怎么验收的，02为什么两个槽位都填满了，难道默认是计划两个一样的？到了04怎么又变了，还有现在不需要拖拽的截图` | 能理解。这是教程主流程图组验收失守，不是玩家要重新看拖拽姿态。Mage Wars 教程最终图组不得把 foundation Board 图、拖拽 / 缩放诊断图、旧图或其它测试文件截图混进主流程序号；计划步骤必须按真实教程链重新编号，并在截图前断言 `1/2` 时只有一张草稿牌、`2/2` 时两张来源牌可区分。拖拽自由视窗证据应放独立 Board / 视窗验证组，不占教程主流程图组。 |
| `单位不是上下排列吗，怎么变横着的了` / `上下排列的意思不是整体上下，而是格子划分左右两边，然后同阵营上下` / `不用重叠啊，就和之前一样啊` / `还有一列三个超出自动换列` | Mage Wars 同格单位 / 法师实体的当前桌面基线是复合轴：每个格子先按固定席位划分左右 owner lane；同席位多个单位再在自己的 lane 内上下分行排列，并保持卡牌 / 肖像可读尺寸。桌面压力态每个 owner lane 一列最多 3 个，超过先自动换列，例如双方各 6 个时每边应为 2 列 × 3 行；不得把“上下排列”误解成双方整体上下分区，也不得把“上下”实现成负边距、absolute、transform 错位或只露角的重叠压叠；换列仍不足时才用滚动、分页 / 展开或焦点检视承接。E2E 必须分别断言 owner lane 为左右、lane 内排列为上下、列数、每列最多 3 个、相邻和跨列实体不重叠、无动作浮层遮挡。 |
| `为什么还是没有选择法师的流程，新建魔法书后怎么选择法师呢` | 能理解。之前把“构筑器里不能常驻第二套法师切换器”误扩成“新建法术书也不需要绑定法师选择”。正确流程是：选书页或构筑器库里的 `+` 先打开绑定法师候选层，玩家点一个法师后，才用该法师标准起始书进入新草稿；构筑器内部仍不能常驻第二套法师切换器。送验前必须检查选书页 `+`、构筑器顶部 `+` 和展开库 `+` 都带 `data-new-spellbook-mage-choice="required"`，E2E 截图必须包含 `新建法术书-先选择绑定法师`，保存后的命名书 `mageId` 必须等于玩家选择的法师。 |
| `当前法术书是什么鬼，现在选中的不就是当前的吗，为什么还要一个当前` | 能理解。被高亮 / 选中的法术书本身已经表达正在使用或正在编辑，界面标题、按钮、说明和筛选项不得再写 `当前法术书`、`当前书`、`当前法师法术书库`、`编辑当前书`、`更新当前副本`、`给当前书取名`、`新书从当前书` 或 `当前书内`。可见文案统一为 `法术书库`、`法术书清单`、`编辑选中书`、`更新选中副本`、`命名副本名称`、`新建法术书`、`另存新书` 和 `书内`；送验前必须由 DOM / E2E 扫描禁止旧词回归。 |
| `法术点又重复，能不能加强规范直接脚本扫描重复？` | 是规范和验收都需要加强，不是只把画面改顺眼。Mage Wars 组书页顶部容量区是总体法术点 `当前 / 上限` 的唯一 owner；右侧标题、摘要、详情层、卡池按钮和说明文案不得再复写同一个 `120 / 120` 或 `法术点` 容量读数。送验前必须运行 `.spec/tools/scan-ui-duplicate-owners.mjs` 或等价 DOM / E2E 扫描，证明默认态只有一个总体容量 owner。 |
| `为什么新增一行没必要吧，不是点击弹窗能解决的事情吗，炉石不是你这样设计的吧` | 能理解。成熟组牌页的详情 / 预览通常由当前英雄、职业、卡牌或清单本体承接，而不是新增一行同权重按钮。Mage Wars 组书页中，当前法师本体点击打开详情；卡牌本体点击加入或预览；右侧清单行承担数量和移除。新增独立行、独立 `详情` 按钮或解释性控制条，默认判为交互 owner 漂移。 |
| `右边要不要禁止简单几何，圆球不能直接显示卡图吗` | 右侧法术书清单里的每一行代表具体法术牌，身份 owner 应优先使用真实卡图缩略或正式卡背。不得用蓝色圆球、首字、抽象几何、普通 icon 或类型色块代替卡图；类型 / 学派只作辅助文字，不作卡牌身份。 |
| `每一个他有而你没有的细节，你都要问为什么没有；每一个你有而他没有的，你要问为什么你要加？` | 能理解。Mage Wars 组书页参考 Hearthstone 时必须交正反对照：炉石有的卡池主视觉、右侧 deck list、紧凑筛选、法力费用筛选、已选 deck / hero owner、完成动作和导入 / 保存降权入口，逐项判断 Mage Wars 是否缺失；Mage Wars 多出来的法术书库、命名副本、法术点、训练 / 相斥、横向墙牌和真实缩略图，逐项用 Mage Wars 规则或用户纠偏解释，解释不了就删除 / 折叠 / 降权。默认态不得再出现大管理栏、说明文案、第二个完成区、规则说明块或第二套“全部 / 书内 / 可加入 / 墙体”范围按钮。 |
| `每一步你都要问自己一遍，为什么别人没有的我必须要有，比如现在的编辑并保存` | 能理解。Mage Wars 选书页标准起始书卡已经通过点击本体完成“选择这本书”；成熟 deck / loadout 选择页不会在每个预设卡下面常驻一个同权重 `编辑并另存` 次按钮。标准书需要编辑时，应先选中该书，再用统一的 `编辑选中书` 入口进入构筑器；保存 / 命名副本在构筑器内完成。送验前代码和 E2E 必须断言选书页不存在 `mage-wars-mage-selection-edit-standard-spellbook-*` 和 `编辑并另存`。 |
| `点击使用是不是也是废话，流程是不是不够硬，这是通用的ui设计skill` | 是。`点击使用` 不是状态，只是在可点击卡片旁复述“它能点”；选中书已经有描边、P1/P2 标记和 `aria-pressed` 这类反馈时，`已使用` 也不应再做同级文字状态。Mage Wars 选书页标准书 / 命名副本卡只保留法术书身份、绑定法师和会影响选择的差异读数；点击动作由卡片本体承担。送验前代码和 E2E 必须断言选书页不存在 `点击使用`、`Click to use`、`已使用` 和 `In use`。 |
| `不是禁止一个，是每个ui元素再添加时都要好好反思，应该在设计的时候就考虑到啊` | 能理解。这是设计阶段门禁失守，不是某个按钮或词的问题。Mage Wars 选书 / 组书送验前必须逐项审所有常驻元素：它承担哪一个玩家任务、成熟参考有没有同类元素、Mage Wars 规则 / 素材 / 保存模型为什么需要它、是否能由法术书本体、法师本体、右侧清单、弹层或无障碍文本承接。说不清的按钮、标签、徽章、输入、状态和说明不进入截图。 |
| `组书的卡牌太小了，看不清` | 组书卡池主视觉是法术牌本体；玩家看不清牌面时，不能靠卡外文字补票。Mage Wars 组书页必须先把卡池卡牌放到可读尺寸，再用滚动 / 筛选承接数量；E2E 需要量普通法术牌真实像素宽高，并确认横向墙牌仍按官方比例显示。 |
| `点哪里显示的法师详情用红圈圈出来，或者直接补充选择法师的端到端` | 法师详情入口仍由已选法师主控本体承接，不能新增同级详情按钮；但送验图必须让用户一眼看出入口，E2E 图组应在组书默认态用红圈标注该法师主控，且同时截图入口可见态和详情打开态。 |
| `DIY标记怎么也没有，怎么参考的召唤师战争` | Summoner Wars 可迁移不变量是“玩家自定义 / 命名保存副本在卡本体上有可见 DIY 身份标记”，不是复制视觉皮肤。Mage Wars 命名副本在选书页库卡和组书法术书库行都必须显示 DIY 徽章；标准起始书不显示 DIY，DIY 不替代法术书名称、绑定法师或数量读数。 |
| `明显有问题哦` | 若截图第一眼仍像设计说明页、筛选墙、入口页、空卡池或少数散卡样本页，自动断言 `PASS` 必须撤销。Mage Wars 组书页送验前必须人工复看：外部成熟参考不能停在导入 / 选英雄入口，筛选区不能把法术牌库推到下半屏，当前视口里完整可见的卡池行必须由真实卡面连续填满，墙体牌按正式横向牌比例显示，不得塞进竖牌壳产生黑边。 |
| `生命、魔力、护盾，这些所有人都一样的东西，根本没必要出现，你居然重复了三次` | Mage Wars 选书 / setup gate 先判断信息是否影响选择。学徒法师生命 24、初始法力 10、聚魔 10 这类全员相同基础属性不是选择差异，不得在双方用书、候选卡、摘要或 chip 中重复展示；法术书数量若各法师不同可以保留。送验前按 `ui-change-gates.md` 用代码检索、DOM 文本扫描或 E2E 断言确认无额外 UI 复写。 |
| `规则上有手牌吗` | Mage Wars 这里用 `法术书`、`已计划法术`、`弃牌堆`、`对手已计划法术卡背`，不得写成手牌或默认持牌区。 |
| `不要凭直觉设计，一切基于规则` | 每个常驻对象必须能回到规则或用户标注：法术书、已计划、弃牌堆、骰子、效果骰、token、法师战场实体、玩家 / 法师提示卡、法师牌详情、区域格。 |
| `开放式设计 / 场地直选优先` | 能由棋盘格、场上卡、token、骰子等真实对象承接的动作，优先让本体高亮和直选；不要新增代理面板、问号块或第二目标卡。 |
| `玩家提示卡在场地，不应该是角色头像这个吗` | 当前玩家是谁、当前玩家下一步做什么，默认挂在当前法师头像 / 角色 HUD 上；竞技场和场上卡只承接来源、合法目标、token、骰子和结果高亮。不得把“选择目标 / 行动中”这类玩家提示做成卡在地图中央或顶部的第二提示条。 |
| `法师的提示卡，和代表法师本人的卡不是一个东西` | 必须先拆 `玩家 / 法师规则提示卡`、`法师战场实体 / 法师本体`、`法师规则 / 提示卡详情` 三行对象矩阵。核心不是固定位置，而是提示 / 参考、战场实体、详情三种职责不能混成一个对象。 |
| `场地里的红框牌密密麻麻写满了字是提示卡` | 看到密集规则文字时，先裁定它是提示 / 规则参考，不得因为它叫 mage card 就放进竞技场当法师本人。当前 v80 基线用人物可识别素材承载场地本体。 |
| `规则提示卡和玩家卡图的位置错了，应该交换` | 这是 Mage Wars v80 基线裁定，不是全局 UI 规范：场地红框位置 = 人物 / 肖像法师本体；玩家面板 / HUD 红框位置 = 密集文字规则提示卡。后续如果改布局，必须先解释新承载如何不再混淆这两个职责。 |
| `地图是最下层，不要被底图支配` | 竞技场底图是底层，不是不可覆盖矩形；保护格子语义、场上实体、token、骰子和关键文字，不保护低权重石砖纹理。 |
| `弃牌堆是给人看的吗` | 弃牌堆是公开归档入口，不是隐藏信息；默认显示所属、数量和紧凑顶牌正面 / 半露正面。降权用尺寸、透明度、角度、边缘位置和“归档”语义，不得用卡背把公开信息画成未知牌。除非当前规则步骤要从中选择，不常驻展开完整清单或正面大卡。 |
| `确认使用的位置真的需要吗` / `别显示确认了` | 没有规则授权的确认 / 执行 / 取消常驻按钮不得出现；选中来源后应高亮合法目标并直接承接动作。 |
| `骰子、token 都被你省略掉了` | 用户点名的规则信息不能为了干净而删除，但必须先辨别承载职责：攻击骰、效果骰、燃烧 token、守卫 / 行动 token 必须在饱和态可见；伤害是连续数值状态，当前真实 Board 用本体受伤遮罩 + 贴宿主剩余 / 总生命读数承载，不强制显示物理伤害 token 图。 |
| `伤害 token 没必要，现代 UI 代替更合适` | 伤害不是必须复刻的离散 token 物件；送验前检查伤害是否贴在受伤对象本体、数值是否可读、是否来自正式 damage 状态、是否采用同游戏或平台成熟样本中的条 / 遮罩 / 读数，而不是孤立 HUD 文本、右下角圆形数字球或无来源通用 icon。 |
| `守卫必须用 token，伤害可以走现代 UI` | 先按状态职责判别，不按“有没有 token 图”机械照搬：守卫 / 护卫是离散规则身份、行动义务或目标保护关系，必须有真实 token 图或等价正式状态物件；守卫状态和守卫动作入口都贴宿主中下方，不能做成右上角按钮堆、护盾 icon 或小卡片壳。伤害是连续累计数值，优先用贴宿主的进度、遮罩或生命读数，禁止用右下角圆球把它伪装成另一种 token。 |
| `护盾、爱心，右下角圆球都是什么鬼` | 这是 Mage Wars 的素材语法纠偏：守卫 / 状态不得用通用护盾或爱心图标替代真实 token；治疗 / 能力动作若没有正式 token，应优先用来源牌面、法师头像、正式对象或文字 fallback 承载；伤害 / 血量参考 Summoner Wars 的受伤遮罩 + 剩余 / 总生命读数，不用右下角圆形徽章。 |
| `守卫不是说放中间下方吗？现在上面守卫底下那个是什么鬼东西？怎么有个小卡片在那里？` | 守卫不是动作栏里的小卡片按钮，也不是和其它能力卡堆在一起的竖向控件；守卫状态 token 和可点击守卫动作 token 都要贴选中宿主中下方。其它能力入口不能贴守卫附近，也不能锚到格子角落或顶部提示横幅；应进入屏幕中下统一动作 dock，来源仍由单位 / 法师本体选中框表达。 |
| `为什么有个守卫单位的小牌` | 先判断它是否真是规则对象：守卫只能是守卫 token，单位能力入口不是场上小牌。能力触发应复用 Summoner Wars 的职责分层：来源本体可选 / 选中；顶部横幅只做描述；能力按钮在屏幕中下统一动作 dock；目标回到棋盘本体高亮。测试必须证明能力入口没有 `source-card` 缩略图、没有图片小卡片，且 placement 是 `middle-lower-action-dock`。 |
| `能力按钮改到屏幕中下位置吧，上面显示横幅代表描述` | Summoner Wars 的顶部横幅是状态 / 描述提示，不是按钮容器；Mage Wars 能力入口必须放在屏幕中下统一动作 dock。顶部区域只保留阶段 / 描述提示和必要视图开关，不能再把能力按钮放回顶部横幅。 |
| `伤害的显示要和召唤师保持一致，不要给我乱搞。也加一个眼睛来控制显示` | Mage Wars 伤害读数沿用 Summoner Wars 的显隐合同：红色受伤遮罩常驻在受伤对象本体，剩余 / 总生命读数默认 hover / 聚焦显示，眼睛按钮切换全场生命读数常显；不得把读数改成永久压牌面的自创样式。 |
| `墙的法术是哪条规范让你不直接把墙的牌摆上去的` | 没有规范授权用泛化墙条替代正式墙牌。墙体目标仍是区域边界，点击和高亮锚在边界；结算后玩家看到的墙体对象必须来自源墙法术牌面 / 墙牌素材，贴在对应边界上，并由 E2E 断言 `sourceSpellCardId` 的墙牌预览可见。 |
| `描边不贴边` | 这是几何问题，不是“整格高亮 vs 对象高亮”的语义问题；检查目标描边是否沿目标卡牌 / 法师本体可见边界，不能用外扩悬浮框、CSS outline 或圆角不一致的框冒充贴边。 |
| `选中没有更好的效果吗，避免选中等效果改变布局` | 选中态只能描边、抬升、发光、短状态贴附或临时检视，不得生成左右特大牌、第二实体或挤压布局。 |
| `你看不出布局的问题吗，下面为什么还有空隙` | 删除底部文案、容量条、按钮或面板后，释放出的空间应回到法术书、已计划法术、弃牌堆、分页或回合结束等真实对象；但这只在本轮已授权布局稿、布局实现或送验收口时触发重排。若当前任务是技能、特效、流程或测试修复，只能记录为布局风险 / 后续项，不得顺手重构 HUD 或移动端承载。 |
| `手牌现在也有问题，计划牌也是你是真的看不出来吗` | 用户口语说“手牌”时，若标注或上下文指向底部可用牌列，按规则术语映射为 `法术书牌列`；不得用“规则没有手牌”回避布局问题。送验前必须同时审 `法术书牌列` 和 `已计划法术`：它们要可读、可辨、各有槽位，不得和分页、弃牌、回合结束互相挤压。 |
| `底部留一点空隙，然后 1366×768 为了适配这个分辨率整体为什么都被改小了，之前的大小挺不错啊不能维持比例吗` / `现在应该可以显示六张手牌了` | 这是 Mage Wars 对项目通用响应式规范的专项 drift-check：1366×768 这类桌面压力态必须保持桌面完整 HUD、6 张法术书、2 个计划槽和主动作可读；不得用减少法术书承载量或整层 UI scale 把底部系统整体压小来冒充适配。底部可以保留约 8px 安全空隙避免贴边裁切，但不能形成空带；具体裁决和跨游戏验收口径以 `ui-responsive-layout.md` 为唯一规范正文。 |
| `这种基础的东西我不希望每次再说` | 基础布局自审必须前置：空隙、挤压、主次、可读性、槽位分离、标签/按钮归属、计划牌是否像当前来源。只要自己能看出问题，不得打开人工验收图，也不得停在“候选”。 |
| `地图缩放比例和眼睛，开关生命显示的，同层级不重叠原则` | 这是项目 `ui-change-gates.md` 的同层级控件独立槽位规则在 Mage Wars 的 drift-check：地图缩放读数和全场生命显示眼睛同属左上工具层，必须各有可见槽位；不能靠 z-index、透明点击区或“刚好没点到”遮掩重叠。送验前用整屏图和矩形量测同时证明眼睛、缩放读数、对手观察眼睛和法师提示卡放大镜不相交且贴在正确承载上。 |
| `卡牌点击没有放大，并且卡牌根本无法点击，分类标签也是，都是假的吗` | 能理解。这是 Mage Wars 运行时默认浏览态失守，不是单个按钮样式问题。法术书牌列、已计划法术、弃牌堆和公开卡牌在非计划 / 非施法阶段仍要能检视或放大；分类标签和分页必须真实改变可见集合。送验前必须检查父容器没有用 `pointer-events` 或透明层吞掉子控件，且 E2E 同时覆盖默认浏览态点击放大、分类切换、分页目标出现，以及计划态点牌进入计划。只测计划态选牌不能证明基础流程跑通。 |
| `放大镜为什么不加上` | 能理解。规划 / 施法 / 目标选择这类操作态不能把卡面单击改成放大，否则会抢走选牌、施法或选目标主流程；但也不能因此没有检视入口。Mage Wars 可见法术书牌、已计划法术、场上卡和附件卡只要同时有主规则动作和可检视牌面，就必须按 `ui-change-gates.md` 提供独立放大镜、右键、长按或等价次级入口；E2E 要证明点放大镜打开大图且不改变已选数量 / 目标来源，点卡面仍执行计划 / 施法 / 选目标。 |
| `看对方弃牌是切换视角而不是新增 UI` | 这是通用 UI / 教程规范在 Mage Wars 的执行失守。Mage Wars 查看对手公开弃牌必须复用对手公开视角：点击对手 HUD 左上眼睛后，同一个 `mw-discard` 主弃牌槽切换到对手 owner；返回自己视角后该槽位恢复己方 owner。不得再新增 `mw-opponent-discard`、`mage-wars-opponent-discard-pile` 或任何平行对手弃牌堆 UI。教程和 E2E 必须截图并断言“切换前己方弃牌 -> 点击观察 -> 同槽位显示对手弃牌 -> 返回己方”，同时负向断言旧副本锚点不存在。 |
| `返回自己视角 不能放中间吗，大杀四方看了没有` | Mage Wars 对手公开视角必须按 Smash Up 已成熟的公开视角语法执行：点击对手 HUD 眼睛后，页面上方居中显示当前正在看对手公开信息的横幅和“返回自己视角”按钮；不得把返回入口随手贴到左上角、弃牌槽或其它局部代理 UI。E2E 必须量同一 `mage-wars-public-view-banner` 外壳和内层按钮组水平居中。 |
| `动条里的“进行守卫”。守卫标记是结算后的状态，不是要先点击的技能或目标。` | Mage Wars 守卫动作入口只写规则动作短名“守卫”；教程规则句优先取规则书原文，UI 操作句只补“点哪个单位、点哪个行动条按钮”。守卫标记 / token 只在结算结果态显示为状态，不得被按钮、目标候选或教程长解释写成玩家先点击的技能。 |
| `地图无法自由拖拽缩放，都有召唤师战争这种成熟参考了为什么还能犯错，是不是要提取通用组件` | 能理解。Mage Wars 竞技场必须先对照 Summoner Wars 的成熟自由查看与卡牌放大实现，抽出可复用不变量：同一坐标父容器、拖拽阈值、滚轮 / 触控缩放、缩放读数、拖拽后抑制误点击、对象点击不被视窗层永久吞掉。若平台已有共享视窗或灯箱组件，Mage Wars 默认应复用或抽共享；不复用必须写规则 / 素材 / 输入模型差异。送验前 E2E 要实际拖拽、缩放，并证明缩放后区域 / 法师 / 墙体 / 卡牌热区仍贴竞技场。 |
| `点击计划槽也要能去掉计划而不是必须点原卡牌` | 这是 Mage Wars 对项目通用“可逆选择由结果槽位承接”的专项 drift-check。计划阶段的草稿牌已经进入两个已计划法术槽位，槽位本体必须能删除对应草稿；不得强迫玩家回到法术书原卡再点一次。放大镜仍是独立检视入口，点放大镜不得改变计划数量，点槽位卡牌本体不得打开放大层。 |
| `计划那一步有问题啊，现在教程skill有问题吗，根本不对劲啊，应该这一步是什么就是什么，而不是描述一堆，和实际操作对不上` | 这是通用教程规范在 Mage Wars 的执行失守。计划段必须按当前真实动作拆分：点分类、翻页、选第一张、点分类、翻页、选第二张、确认计划；每步文案、高亮、allowed target 和截图都只对应当前动作，不把后续法术效果、下一阶段或多个“然后”塞进一张教程卡。 |
| `每次设计完毕都用我的话反思一下` | 最终回复和审计必须有“按用户原话自审”的明确结论；不能只报几何、DOM 或 OpenSpec 通过。 |

## 分页专项裁决

- 按钮本体：保留用户已认可 / 已标注的样式和贴近法术书的位置；不要把按钮缩成小圆点、挪到屏幕底边或改成无关方向。
- 页码读数：是附属信息，优先轻量放在按钮附近或容量读数中；不得独立撑出大栏。
- 卡牌优先级：法术书当前页卡面比页码更重要；分页调整不得让卡牌变小、不可读或被底图挤走。
- 热区与可见面分离：实现阶段可用透明热区补点击面积，但设计稿里的可见按钮面不能为了触控下限变成主对象。

## 法术书 / 已计划 / 底部空间裁决

本节只描述 Mage Wars PC 基线和规则对象职责。若改动触及移动端、横屏、响应式、承载替换或 PC/移动同构，先回到 `ui-responsive-layout.md` 裁决；本节不得作为新增移动布局、隐藏 PC 同源承载或重排 HUD 的授权来源。

- 法术书牌列是当前可支配对象集合，不是底边装饰条。它必须在 PC 100% 缩放下能读出卡面主体，并和分类标签、分页 rail 形成同一浏览组件。
- 已计划法术是当前可施放来源，不是右下角挂件。它必须有独立计划槽，至少能看清卡面、数量和当前来源态；不得被分页、弃牌堆或回合结束按钮挤成一团。
- 弃牌堆是公开归档，不是隐藏区。PC 主态默认用紧凑顶牌正面 / 半露正面 + 数量表达可检视内容；只有对手已计划法术、未公开法术书内容和隐性结界这类隐藏信息才强制卡背。
- 运行时牌桌 UI 不使用 16:9 内框。玩家实际看的状态、法术书牌列、已计划法术、弃牌和操作入口，按类似 Unity Canvas 的整屏九宫格锚点做左上、右上、左下、底部中段、右下等对齐；锚点只定义对齐关系，不是给这些 UI 再造一个摆放范围。当前桌面基线里，己方 HUD / 规则提示卡位于左下独立层，不和底部法术书牌列同排、不占法术书列；对手 HUD / 规则提示卡位于右上；对手已计划卡背位于右上对手 HUD 左侧相邻，只显示卡背 / 数量且不遮挡 HUD。底部基线只服务法术书牌列、分页 rail、已计划法术 / 计划确认区和回合入口。用户口语说“手牌”时若指底部可用牌列，按规则术语映射为法术书牌列后继续处理布局问题，不得用“规则没有手牌”回避。
- 己方 HUD 是左下顶层界面锚点，不属于竞技场实体布局；不得因为 A2、首列或任意场上实体存在而启用横向安全偏移、漂移到中场或预留无职责大带。若 HUD 与当前任务对象发生冲突，回到通用 UI 分层规范处理层级、命中区、同层 HUD 重排、当前目标提权或临时收纳；HUD 提示卡本体是视觉承载，不是整卡点击按钮，放大由右上放大镜承担，不在 Mage Wars 专项里用“避让场上实体”建立第二套规则。
- 默认地图相机不是内框。PC 默认缩放优先让正式 4x3 / 12 区域完整可见，并避开底部法术书牌列、已计划法术 / 计划确认区和回合入口的实际占位；该占位只服务默认镜头计算，不允许派生成安全壳、固定画布或 UI 摆放范围。放大 / 拖拽态再要求地图内容覆盖真实视窗、不露外层黑框，且玩家界面锚点不跟随地图拖动。
- 底部空隙必须有职责。若只是删掉底部文案 / 容量读数 / 面板后留下的空带，在已授权布局收口任务中必须回到上位 UI 规范和对象矩阵重排，直到空间回到法术书、已计划法术、弃牌堆、分页或回合结束等真实对象；非布局任务不得借此顺手改版。
- 槽位分离必须用整屏图验收：己方法师状态、左侧分类标签、法术书牌列、分页 rail、已计划法术、弃牌堆和回合结束都要同时可见，并逐项检查是否相交、抢焦点或职责不清。
- 几何不相交只是底线。若玩家第一眼仍觉得牌列像硬塞、计划牌像角落挂件、底部留白像死区，当前稿仍是 `REVISE`。

## 纠正覆盖账本

- `docs/games/mage-wars/design/reference/user-correction-traceability-ledger.md` 记录用户已指出的 Mage Wars 设计纠正、规则 / 素材证据、下一稿门禁和送验前检查方式。
- 该文件角色是 `drift-check / evidence`：它不能创造新规范，只能引用本 skill、上游设计 / 审计入口、规则合同和素材合同。
- 每次用户新增纠正，先判断是否已经被该账本覆盖；没有覆盖时先补账本或回到 canonical-source 补规则，再改 artifact。
- 每次送验前必须逐项复核账本里的 `必须检查` 列；任一项未覆盖或图面失败，当前稿不得 `AI_PASS`，也不得打开人工验收。

## 送验最低回执

每次打开给用户人工验收前，写清：

- 本轮用户当前反馈对象是什么。
- 本轮是否授权 UI 布局 / 设计稿 / 送验收口；若只是技能、特效、流程或测试任务，发现布局问题只能记录为风险 / 后续项，不得顺手改已定 HUD 或移动布局。
- 若涉及 PC/移动、横屏或响应式，`ui-responsive-layout.md` 的裁决结论是什么；本 skill 只补 Mage Wars 专项 drift-check。
- 哪些元素保持原样，哪些元素被改动。
- 是否删掉或弱化了用户点名元素；如果有，必须说明规则阶段互斥或用户授权。
- 规则对象覆盖矩阵是否完整；若有 `out-of-scope`、`blocked` 或 `approved-programmatic`，必须逐项说明现实原因。
- 用用户原话逐条自审的结论。
- 纠正覆盖账本逐项自审的结论；不得只挑当前一条反馈说明。
- 基础布局自审结论：底部是否还有无职责空隙、法术书牌列是否像当前可支配对象、已计划法术是否像当前来源、各槽位是否仍在抢位；任一项未达标时，布局 / 设计稿 / 送验收口任务必须回到上位规范和对象矩阵处理，非布局任务只记录风险 / 后续项，不得顺手重构。
- `AI_PASS` 只表示可人工看图，不表示已批准实现。
- 回执必须短，默认不交长文字版 UI 设计；真正给用户判断的是图面本身。
